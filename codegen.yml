overwrite: true
schema: "https://beta.frikanalen.no/graphql"
documents: "src/**/*.graphql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - typed-document-node