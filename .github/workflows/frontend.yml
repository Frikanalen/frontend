name: Frontend build

on:
  push:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Release PR or GitHub Release (main only)
        id: release
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: googleapis/release-please-action@v4
        with:
          release-type: node

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract production Docker metadata
        id: meta_production
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/frikanalen/frontend
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=${{ steps.release.outputs.tag_name }},enable=${{ github.ref == 'refs/heads/main' && steps.release.outputs.release_created == 'true' }}

      - name: Extract staging Docker metadata
        id: meta_staging
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/frikanalen/frontend
          tags: |
            type=ref,event=branch,suffix=-staging
            type=sha,prefix=sha-,suffix=-staging,format=short
            type=raw,value=staging,enable={{is_default_branch}}
            type=raw,value=${{ steps.release.outputs.tag_name }}-staging,enable=${{ github.ref == 'refs/heads/main' && steps.release.outputs.release_created == 'true' }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push production image
        uses: docker/build-push-action@v5
        with:
          context: .
          build-args: |
            NEXT_PUBLIC_DJANGO_URL=https://frikanalen.no
          push: true
          tags: ${{ steps.meta_production.outputs.tags }}
          labels: ${{ steps.meta_production.outputs.labels }}
          cache-from: type=gha,scope=production
          cache-to: type=gha,mode=max,scope=production

      - name: Build and push staging image
        uses: docker/build-push-action@v5
        with:
          context: .
          build-args: |
            NEXT_PUBLIC_DJANGO_URL=https://staging.frikanalen.no
          push: true
          tags: ${{ steps.meta_staging.outputs.tags }}
          labels: ${{ steps.meta_staging.outputs.labels }}
          cache-from: type=gha,scope=staging
          cache-to: type=gha,mode=max,scope=staging
